# Баги

- 2026-09-25 — pyflakes: неиспользуемые импорты (`ser_tariff` в admin.py, `admin_required` в api.py, `Offer` в services.py) — исправлено.
- 2026-09-25 — На базах, созданных до удаления формы, «Получить ссылку» падало: `offer_links.form_enabled` NOT NULL без default — исправлено (поле возвращено в модель).
- 2026-09-25 — На экране входа за формой проглядывали боковое меню и кнопка поддержки (`hidden` перебивался CSS) — исправлено.
- 2026-09-25 — Ссылка-приглашение строилась без подпапки (`host_url`) — исправлено на `url_root`.
